class ItemProductModel {
  final bool isActive;
  final String title, amount, discount, image;

  const ItemProductModel({
    required this.isActive,
    required this.title,
    required this.amount,
    required this.discount,
    required this.image,
  });
}
